const chevron = document.querySelector(".chevron");
const img = document.querySelectorAll("img");
//list형으로 반환.
console.log(img);

function nextpage(event) {
  console.log(img.indexof());
  var num = img.indexo;
}

function init() {
  if (chevron.addEventListener("click", nextpage)) {
  } else {
  }
}

init();
